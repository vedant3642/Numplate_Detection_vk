# =============================================================================
# NumPlate Detection — Python Dependencies
# =============================================================================

# --- Core detection & OCR ---
ultralytics>=8.0.0         # YOLOv8 / YOLOv11 (vehicle + plate detectors)
# easyocr>=1.6.2             # Default OCR backend
paddleocr>=2.7.0         # Alternative OCR backend (uncomment to use)
paddlepaddle>=2.5.0      # PaddleOCR GPU build; use paddlepaddle-gpu for CUDA

# --- Tracking ---
deep-sort-realtime>=1.3.2  # DeepSORT multi-object tracker

# --- Computer vision ---
opencv-python>=4.8.0       # Frame I/O and preprocessing
numpy>=1.24.0

# --- Storage & export ---
sqlalchemy>=2.0.0          # ORM + DB sessions
pandas>=2.0.0              # CSV export

# --- Configuration ---
pyyaml>=6.0.0              # YAML config parsing

# --- Dataset acquisition (optional — only needed for download_dataset.py) ---
roboflow>=1.1.0

# --- Training augmentation (optional — only needed for train_plate_detector.py) ---
albumentations>=1.3.0

# --- Utilities ---
tqdm>=4.65.0               # Progress bars in training output
